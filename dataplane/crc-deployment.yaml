# CRC EDPM Deployment
# Triggers Ansible deployment to EDPM compute nodes
---
apiVersion: dataplane.openstack.org/v1beta1
kind: OpenStackDataPlaneDeployment
metadata:
  name: edpm-deployment-crc
  namespace: openstack
spec:
  nodeSets:
    - openstack-edpm-crc
